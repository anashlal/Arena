package setting;

import static org.junit.Assert.*;

import org.junit.Assert;
import org.junit.Test;

import game.Arena.Calculation;
import game.Arena.Data;
import game.Arena.PlayMore;
import game.Arena.RandomFight;
import game.Arena.oppening;


public class arena_test {
/*
	@Test
	public void Test_Add_true() {
		 double firstnumber = 9.0;
		 double secondnumber = 1.0;
		double expected = 10.0;
		double actual = calcalator.Add(firstnumber, secondnumber);
		Assert.assertEquals(expected, actual, 0);

	
	@Test
	public int IsPlayerDead_test() {
		int Health = 11 ;
		int expecteds= 11 ;
		int actuals =Data.getHealth();
		
		Assert.assertEquals(expecteds, actuals);
		return Health;
	}
	@Test
	public int getPowerAfterIncrease_test(int increaseValue)
	{
	    int expected=10;
	    int actuals =getPowerAfterIncrease_test(increaseValue);
	    Assert.assertEquals(expected, actuals);
		return increaseValue;
		
		
	}
	
	
	@Test
	public void DecreaseHealth_test(int value) {
	
	value = 11;
	int expecteds = 111;
	int increaseValue = 0;
	int actuals =getPowerAfterIncrease_test(increaseValue); 
	Assert.assertEquals(expecteds, actuals,increaseValue);
	
	}
*/
}
